using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InputController : MonoBehaviour  // ну будет типа эээ назначать клавиши там может быть хз
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
